/**
 * ⚠️  DEPRECATED — do not import from this file.
 *
 * The axios instance and backend URL are now managed centrally via ApiContext.
 * Use the `useApi()` hook instead:
 *
 *   import { useApi } from '../context/ApiContext';
 *   const { api } = useApi();
 */
throw new Error(
  '[api.js] This module is deprecated. Import useApi from context/ApiContext instead.'
);
